<section class="sitewide-comments">
  <% @comments.each do |comment| %>
  <div class="comment">
    <p class="comment-owner">
      <strong>
        <%= comment.user.email %>
      </strong>

      <span class="comment-created small">
        <%= time_ago_in_words(comment.created_at) %> ago
      </span>

      <% if logged_in?  %>
      <span class="edit-comment">
        <%= link_to 'edit',  edit_comment_path(comment.submit, comment) %>
      </span>

      <span class="delete-comment">
        <%= link_to 'delete', comment_path(comment.submit, comment), method: :delete, data: { confirm: 'Are you sure?' } %>
      </span>
     
      <% end %>
    </p>

    <p>
      <%= comment.body %>
    </p>
  </div>
  <% end %>
</section>
